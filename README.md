## Initial code description

`code/01_make_output1.R`

  - cleaning and editing the data
  - steps are saved as `.rds` objects in `output/` folder

`code/02_render_report.R`

  - renders `Report2.Rmd`

`report.Rmd`

  - reads .rds files generated by `code/01_make_output1.R`
    - .rds files that are used in the report are `Average_PM2.5.rds` and `Fulton_data.rds`
  - makes a table and a boxplot 